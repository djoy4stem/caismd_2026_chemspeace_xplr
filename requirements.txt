# ── How to install ───────────────────────────────────────────────────────────
# Recommended (conda — installs RDKit + all dependencies in one step):
#
#   conda create -n chemspace_xplr python=3.10 --yes
#   conda activate chemspace_xplr
#   conda install -c conda-forge rdkit --yes
#   pip install -r requirements.txt
#
# pip-only fallback (RDKit available on PyPI since 2022):
#
#   pip install -r requirements.txt
#
# Google Colab (run as the first cell):
#
#   !pip install -r requirements.txt
#
# Verify after installation:
#   python -c "import rdkit, umap, sklearn, pandas, mols2grid; print('✅ All dependencies loaded.')"
# ─────────────────────────────────────────────────────────────────────────────

# Core scientific stack
numpy>=1.26.0,<3.0.0
pandas>=2.0.0,<4.0.0
scipy>=1.11.0,<2.0.0

# RDKit — via conda (preferred) or PyPI
rdkit>=2023.9.0

# Dimensionality reduction
scikit-learn>=1.4.0,<2.0.0
umap-learn>=0.5.6,<0.6.0

# Visualization
matplotlib>=3.8.0,<4.0.0
seaborn>=0.13.0,<0.14.0
plotly>=5.0.0,<7.0.0
ipywidgets>=8.1.0,<9.0.0

# Jupyter environment
jupyterlab>=4.1.0
notebook>=7.1.0

# Progress bars
tqdm>=4.66.0,<5.0.0

# Interactive molecule browser (Extension 2)
mols2grid>=2.0.0

